# Port the backend server will run on (Render handles this dynamically, defaults to 3001 locally)
PORT=3001

# The URL of your deployed frontend application (used for CORS configuration)
FRONTEND_URL=https://nexusflow-crm-frontend.onrender.com
